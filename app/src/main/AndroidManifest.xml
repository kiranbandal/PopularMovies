<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.kirangisp.popularmovies" >

    <!-- This App needs an Internet Permission to query the MovieDB API -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- To check if the device is connected to the internet -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!--Movie Posters GridView Activity from the UI Components Library-->
        <activity
            android:name="com.kirangisp.popularmoviesui.MoviePostersActivity">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
    </application>

</manifest>
